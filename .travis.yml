language: php
php:
  - '7.0'
  - hhvm

before_script:
  - cp app/config/parameters.yml.dist app/config/parameters.yml
  - composer install

env:
  global:
    - MIN_PHP=7.0.1
#
#matrix:
#    include:
#        - php: hhvm
#        - php: 5.5
#        - php: 5.6
#          env: deps=high
#        - php: 7.0
#          env: deps=low
#    fast_finish: true
#

script:
  - php bin/phpunit -c app/phpunit.xml --colors=always || (echo -e "\e[41mFAILED\e[0m {}" && $(exit 1));

services:
  - mongodb
  - mysql
