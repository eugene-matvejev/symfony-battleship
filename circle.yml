machine:
    php:
        version: "7.0.4"

dependencies:
    pre:
        - "composer install"

test:
    override:
        - "php bin/phpunit -c phpunit.xml --colors=always --verbose --coverage-clover=coverage.xml"
        - "php bin/behat"
