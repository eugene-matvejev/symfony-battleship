machine:
  php:
    version: 7.0.0RC7

dependencies:
  pre:
    - cp app/config/parameters.yml.dist app/config/parameters.yml

test:
  override:
    - php bin/phpunit -c tests/phpunit.xml --colors=always --verbose --coverage-clover=phpunit-report.xml

after_success:
  - bash <(curl -s https://codecov.io/bash)
